import os from "os";
import path from "path";
// be careful about imports here. It can create a cyclic dependency
import { defineConfig, Api, AppConfig } from "sx/config";

// you can change any sx command triggers here
// see sx/sx-default-config.ts for all available options
defineConfig({});
